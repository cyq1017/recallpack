# API Client Fixture

This small repository snapshot models a request builder used by an internal API
client. Public tests only check that `build_request` returns a mapping with a
path, headers, and timeout field.

The handoff task is to update the request builder to the current project policy.
