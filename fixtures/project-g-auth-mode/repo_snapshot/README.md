# Project G Provider Auth Fixture

This small repo snapshot models an upstream model-provider auth helper.

Public tests only check that `build_upstream_headers` returns a mapping. Hidden
tests check the current provider-key and OAuth header-mode policy.
